# Add DNS Record for karsathi.co.in

Use this prompt to generate or confirm DNS record settings for the domain `karsathi.co.in`.

Provide DNS record details for the requested domain in a provider-friendly format. Use `@` for the root domain. Do not include unrelated analysis.

Inputs:
- Type: DNS record type (A, AAAA, CNAME, TXT, etc.)
- Name: DNS record name or host (use `@` for root)
- Value: DNS record value
- TTL: Time to live (e.g. 1 Hour, 3600, automatic)

Example request:
Type: A
Name: @
Value: 76.76.21.21
TTL: 1 Hour

Expected output:
Type: A
Name: @
Value: 76.76.21.21
TTL: 1 Hour

Confirm the record is for `karsathi.co.in` and keep the response concise.